package model.comparators;

import model.Person;

public class PersonDateComparator implements Comparable<PersonDateComparator>{
    private Person person;
    @Override
    public int compareTo(PersonDateComparator o) {
        return 0;
    }
}
