package model.comparators;

import model.Person;

public class PersonNameComparator implements Comparable<PersonNameComparator>{
    private Person person;
    @Override
    public int compareTo(PersonNameComparator o) {
        return 0;
    }
}
